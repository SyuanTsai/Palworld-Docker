# 共用資料－存檔盤點說明

最新的自動盤點結果請見[據點存檔現況](據點存檔現況.md)；在儲存庫根目錄執行 `.\update-base-records.cmd` 即可更新。

最後盤點：2026-07-29 17:55:36（Asia/Taipei）

## 資料來源

- 世界 GUID：`EC3326BEAC224A53859D47D64EB6C0B0`
- 遊戲版本：`v1.0.1.100619`
- 來源：伺服器目前使用中的 `Level.sav`
- 解析方式：以內建 `palsav` 工具唯讀轉換成暫存 JSON，再讀取據點、建築、工作容器與工作指派資料。
- 盤點結果：1 個公會、5 個據點、5 個據點工作容器。

## 據點判定

據點依 `BaseCampSaveData` 的排列與下列資料交叉辨識：

1. 據點 ID 與座標。
2. 建築所屬的 `base_camp_id_belong_to`。
3. 帕魯暱稱中的 `#01`～`#05`。
4. 第四據點已知的 3 座普通原油提煉機、2 座高壓原油提煉機及 3 座配種牧場。
5. 第五據點已知的 4 座配種牧場、4 座家畜牧場及 12 座大型電能帕魯蛋孵化器。

## 數量與名稱規則

- 建築數量只計入所屬據點 ID 相符的功能建築。
- 屋頂、地基、牆、柱、樓梯、裝飾、告示牌、散落物與帕魯蛋等不列入據點功能設施表。
- 帕魯數量取自各據點 `WorkerDirector` 指向的工作容器；空格不計入。
- 遊戲顯示星級以存檔內部 `Rank - 1` 換算，例如內部 Rank 5 記為 4 星。
- 家畜牧場、配種牧場及固定工作設施的帕魯配置取自 `WorkAssignMap`，不只依暱稱推測。
- 帕魯內部名稱透過[帕魯索引](配種資料/帕魯索引.csv)轉為繁體中文。
- 建築內部名稱參考 [Pocketpair 官方 Technology ID 清單](https://docs.palworldgame.com/settings-and-operation/technologyids/)；專案沿用既有用語，例如「蓄電器」、「大型廚房」及「低溫保鮮飼料箱」。

## 被動詞條對照

本次配種親代使用到的主要內部詞條：

| 內部名稱 | 繁中名稱 |
|---|---|
| `WorldTree_CraftSpeed` | 惡魔之手 |
| `CraftSpeed_up3` | 卓越技藝 |
| `CraftSpeed_up2` | 工匠精神 |
| `Vampire` | 吸血鬼 |

## 限制

- 本盤點是 2026-07-29 17:55:36 的存檔快照；之後搬移帕魯、增拆建築或更換配種親代都不會自動同步到 Markdown。
- 存檔能確認建築、帕魯及固定工作指派，但無法直接判定玩家規劃中的產量目標、庫存安全量與尚未執行的配置。
- 沒有固定指派或工作暱稱的帕魯，只記錄物種與數量，不臆測實際工作。
