# Agent 工作規範

## 變更與提交流程

1. 收到問題或修改需求後，先完成調查、必要的修改與驗證。
2. 完成後先向使用者回報：
   - 問題原因或處理結果
   - 實際變更的檔案與重點
   - 已執行的檢查、測試及其結果
   - 尚未確認的風險或限制
3. 回報完成後，必須等待使用者明確確認，例如「確認」、「可以提交」或「請 commit」。
4. 在收到明確確認以前，不得執行 `git add`、`git commit`、`git push`，也不得建立 Pull Request。
5. 收到確認後，重新檢查 `git status` 與預計提交的差異，只納入本次需求相關的檔案，然後自動建立 commit，不必再詢問一次。
6. 使用者的確認只適用於最近一次已回報的變更內容。若確認後又產生實質修改，必須重新回報並再次等待確認。
7. 建立 commit 後回報 commit hash 與訊息。除非使用者另外明確要求，否則不得 push 或建立 Pull Request。
8. 若使用者只是詢問問題而沒有要求修改，僅回答問題，不建立 commit。

## 據點資料使用規則

1. 處理據點、產線、配種、配方或工作詞條問題前，先讀取 `BaseRecords/README.md`。
2. 回答或修改前，先依問題類型選定資料來源：一律讀取目標據點檔案，並只讀取與問題直接相關的 `BaseRecords/共用資料/` 子資料集或章節。問題跨越多種類型時，可並行讀取所有已判定必要的資源；不得僅因據點檔案列有引用而全量讀取。
3. 跨據點適用的配方、公式、詞條與配種路線只更新共用子資料集，不在每個據點重複保存。
4. 據點檔案只記錄該據點的實際數量、配置、限制與共用規則的例外。
5. 需要直接從容器取得即時資料時，先確定回答問題所需的容器、資源與欄位，再以最窄範圍分別查詢；不得在單一指令中串接並輸出多份彼此無關的設定、日誌或原始資料。
6. 多項資訊若同樣來自 `Level.sav`，底層 `palsav convert` 應在一次轉換中合併本次必要的 `--custom-properties`，避免為每項資訊重複解析同一存檔；轉換後再依問題篩選據點、欄位或章節，不得將完整 JSON 載入對話上下文。
7. `update-base-records.cmd` 只用於更新或核對完整據點現況；查詢單一據點或單一類型資訊時，優先取得對應的摘要欄位或章節。
